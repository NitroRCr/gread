# GitHub Personal Access Token (for fetching API)
GITHUB_TOKEN=your_github_token_here

# OpenAI Compatible API Setup (used by AI SDK)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4o-mini

# Server Port
# PORT=3000

# Minimum stars to auto-sync
# MIN_STARS=10000

# Blobless clone limit
# BLOB_LIMIT=100k

# Sync interval in milliseconds (default 72 hours)
# SYNC_INTERVAL=259200000

# Number of concurrent indexing processes
# SYNC_CONCURRENCY=8

# Path to store SQLite DB and repos
# DATA_PATH=./data

# Requests per minute limit
# RPM_LIMIT=60
